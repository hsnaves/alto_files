-- TinyPup.config,  Last Edit: HGM  October 25, 1979  3:46 PM-- Copyright  Xerox Corporation 1979, 1980PACK DispatcherLocked, TedLocked, QueueLockedTiny;PACK DispatcherHot, QueueIn, TedIn, PupRouterIn, PupChecksums;PACK PupRouterOut, TedOut, PupSockets, QueueOut;PACK PupByteStreams, AltoMesaByteBlt, StreamImpl;PACK PupErrors, DummyForwarder;PACK PupRouterCool, QueueCool, AMCUCool;PACK DispatcherCold, QueueCold, PupRouterCold, TedCold, AMCUCold;TinyPup: CONFIGURATION LINKS: CODE  IMPORTS    FrameDefs, ImageDefs,    ProcessDefs, SegmentDefs, StringDefs, SystemDefs  EXPORTS    Stream, ByteBltDefs,    PupPktDefs, PupStream, PupDefs, CommUtilDefs, BufferDefs,    DriverDefs, PupRouterDefs  CONTROL Boss =    BEGIN    TED: CONFIGURATION  -- Tiny Ethernet Driver      IMPORTS        CommUtilDefs, DriverDefs, BufferDefs      EXPORTS        DriverDefs      CONTROL TedCold =        BEGIN        TedCold;        TedIn;        TedLocked;        TedOut;        END;    PupRouter: CONFIGURATION      IMPORTS        PupDefs, CommUtilDefs, DriverDefs, BufferDefs, PupRouterDefs, StatsDefs      EXPORTS        PupStream, PupDefs, PupRouterDefs      CONTROL PupRouterCold =        BEGIN        PupChecksums;        PupErrors;        PupRouterCold;        PupRouterCool;        PupRouterIn;        PupRouterOut;        END;    AltoMesaByteBlt;    AMCUCold;    AMCUCool;    Boss;    DispatcherCold;    DispatcherLocked;    DispatcherHot;    DummyForwarder;    NameConversion;    PupByteStreams;    PupListeners;    PupRouter;    PupSockets;    PupPktCool;    PupPktHot;    QueueCold;    QueueCool;    QueueLockedTiny;    QueueIn;    QueueOut;    StatsPhoney;    StreamImpl;    TED;    END.