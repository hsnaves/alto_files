-- LaurelNucleus.config edited by Taft; April 14, 1983  10:36 AMDIRECTORY  Resident: FROM "LaurelResident";PACK Resident, LaurelInterrupt, Signaller, DiskIO, Swapper, MDSRegion;LaurelNucleus: CONFIGURATION LINKS: CODE  EXPORTS AllocDefs, BFSDefs, CoreSwapDefs, DirectoryDefs,    DiskDefs, DiskKDDefs, FrameDefs, FrameOps, FSPDefs, ImageDefs,    LoadStateOps, MiscDefs, NucleusOps, ProcessDefs, Region,    SegmentDefs, StreamDefs, StreamScan, StringDefs, SwapperOps, SystemDefs,    TrapDefs, Process, Runtime, Storage, String  CONTROL Wart =    BEGIN        -- Resident Nucleus    Resident;    LaurelInterrupt;    Signaller;    DiskIO;    Swapper;    MDSRegion;        -- Non Resident Nucleus    HyperRegion;    BFS;    Directory;    DiskKD;    Faults;    Files;    FSP;    LoadState;    MesaDebug;    MesaInit;    Miscellaneous;    Modules;    NonResident;    OurProcess;    SegmentsA;    SegmentsB;    StreamsA;    StreamsB;    StreamsC;    StringsA;    StringsB;    Wart;        END....