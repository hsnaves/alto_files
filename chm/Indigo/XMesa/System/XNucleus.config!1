-- Nucleus.config edited by Sandman; Mar 30, 1979 10:15 AM-- XNucleus.config edited for XMesa by Levin; April 16, 1979  6:45 PMPACK Resident, Signaller, DiskIO, Swapper, Interrupt;XNucleus: CONFIGURATION LINKS: CODE  EXPORTS AllocDefs, BFSDefs, BootDefs, CoreSwapDefs, DirectoryDefs,    DiskDefs, DiskKDDefs, FrameDefs, FrameOps, FSPDefs, ImageDefs,    LoadStateOps, MiscDefs, NucleusDefs, ProcessDefs, SegmentDefs,    StreamDefs, StringDefs, SystemDefs, TrapDefs, MemoryOps, LongCodeOps,    XMESA, XMesaDefs  CONTROL Wart =    BEGIN        -- Resident Nucleus    Resident;    Signaller;    DiskIO;    Swapper;    Interrupt;        -- Non Resident Nucleus    BFS;    Directory;    DiskKD;    Files;    FSP;    LoadState;    MesaDebug;    Miscellaneous;    Modules;    NonResident;    Process;    Segments;    StreamsA;    StreamsB;    StringsA;    StringsB;    Wart;        END....