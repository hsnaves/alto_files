-- XMesa.config; edited by Levin, October 18, 1978  1:14 PMPACK Resident, DiskIO, Swapper, Signaller, Keyboard, MesaCompatible;XMesa: CONFIGURATION LINKS: CODE  EXPORTS TableDefs, BcdTabDefs, DiskDefs, DisplayDefs, StringDefs, SegmentDefs, BFSDefs, StreamDefs, IODefs, FontDefs, FSPDefs, SystemDefs, BootDefs, ProcessDefs, ImageDefs, DirectoryDefs, DiskKDDefs, FrameDefs, MiscDefs, TrapDefs, KeyDefs, StreamIO, LoaderDefs, LoadStateDefs, LoaderBcdUtilDefs, TimeDefs, AllocDefs, NucleusDefs, CoreSwapDefs, ForgotDefs, XMesaDefs, XMESA, CodeDefs  CONTROL Wart =    BEGIN    Resident;    Signaller;    DiskIO;    Swapper;    Keyboard;    MesaCompatible;    KeyStreams;    LoaderUtilities;    LoadState;    LoaderBcdUtilities;    Loader;    Process;    Directory;    DiskKD;    BFS;    Miscellaneous;    NonResident;    Segments;    Strings;    Files;    StreamsA;    StreamsB;    FSP;    DebugNub;    StreamIO;    AlFont;    SystemDisplay;    DisplayControl;    TimeConvert;    NubControl;    Allocator;    BcdTab;    BcdMerge;    MakeImageUtilities;    MakeImage;    Wart    END.